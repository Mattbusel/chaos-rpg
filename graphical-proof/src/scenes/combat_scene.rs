//! Combat scene builder — sets up the 3D arena for combat.

use proof_engine::prelude::*;
use crate::state::GameState;

/// Build the combat arena scene.
pub fn build_arena(_state: &GameState, _engine: &mut ProofEngine) {
    // TODO: Build 3D perspective arena
    // - Floor grid of glyphs
    // - Ambient force fields based on room type
    // - Lighting: player and enemy point lights
}
